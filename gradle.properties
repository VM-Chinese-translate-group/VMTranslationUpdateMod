# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx8G
org.gradle.daemon=true
#org.gradle.parallel=true
org.gradle.caching=true
org.gradle.caching.debug=false
#org.gradle.configureondemand=true

# Mod properties
mod.version=5.0.0
mod.group=top.vmctcn.vmtu
mod.id=vmtranslationupdate
mod.name=VMTranslationUpdateMod

# Used for the mod metadata
mod.mc_dep_fabric=[VERSIONED]
mod.mc_dep_forgelike=[VERSIONED]
# Used for the release title. I.e. '1.20.x'
mod.mc_title=[VERSIONED]
# Space separated versions for publishing. I.e. '1.20, 1.20.1'
mod.mc_targets=[VERSIONED]

# Mod setup
deps.mixin_extras=0.4.1
deps.fabric_loader=0.18.4
deps.fabric_api=[VERSIONED]
deps.forge_loader=[VERSIONED]
deps.neoforge_loader=[VERSIONED]

# Mod dependencies
deps.core_version=0.2.17
deps.modmenu=[VERSIONED]
deps.cloth_config=[VERSIONED]
deps.auto_service=1.1.1

# Publishing
publish.modrinth=wvCSIW08
publish.curseforge=890229
publish.version_type=alpha
publish.mod_side=client